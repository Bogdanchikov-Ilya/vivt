const popupLunks = document.querySelectorAll('.popup-link')
const body = document.querySelector('body')
const lockPadding = document.querySelectorAll('.lock-padding')

let unlock = true

const timeout = 600

if(popupLunks.length > 0){
    for(let index = 0; index < popupLunks.length; index++){
        const popupLunk = popupLunks[index];
        popupLunk.addEventListener("clicl", function (e){
            
        })
    }
}